This is the directory for my lab7-Fractal-Tree program.

This is a Java Swing program that draws a recursive fractal tree.  
Each branch splits into two smaller branches until a maximum depth, with decreasing length and angled offsets.  
Creates a tree-like structure using simple lines.  
An example of recursion, graphics, and basic trigonometry.
